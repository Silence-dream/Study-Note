# # 1.
# # 导入math模块，并起个别名叫“m”
# import math as m

# # 使用math库计算2的3次方
# print(m.pow(2, 3))

# 从math模块中导入所有的项（函数、常量等），这种方式可以直接引用math模块中的方法
from math import *
print(sqrt(4))
a = 5
N = 25
print(log(N, a))
